# 【機器學習2021】References & Remarks
Refs:
**李宏毅線上課程** **[ML 2021 Spring](https://speech.ee.ntu.edu.tw/~hylee/ml/2021-spring.php)**
 **[【機器學習2021】(中文版) - YouTube](https://www.youtube.com/playlist?list=PLJV_el3uVTsMhtt7_Y6sgTHGHp1Vb2P2J)**
 https://github.com/ga642381/ML2021-Spring/tree/main
**[https://github.com/1am9trash/Hung_Yi_Lee_ML_2021?tab=readme-ov-file](https://github.com/1am9trash/Hung_Yi_Lee_ML_2021?tab=readme-ov-file)**

HW:
HW1: covid test prediction
HW2: Speech Recognition
HW3: Image Recognition
HW4: Speaker Recognition
HW5: Machine Translation

HW11: 影像分類. Mismatch: Training data 跟 Testing date的分佈不一樣.

自修:
老師上課的內容(在哪?) 跟PyTorch裡面的BatchNorm 的 running_mean / running_var來交叉比對

自動化超參數搜尋（Automatic Hyperparameter Optimization, HPO) ex: Bayesian Optimization
Sharp vs. Flat Minima 
SAM (Sharpness-Aware Minimization): SAM 是這幾年研究中非常有效的做法：$\min _w \max _{\|\epsilon\| \leq \rho} L(w+\epsilon)$ 直覺上就是「找一個附近點都不差的 w」。

關鍵字:
(To be taught): 老師說之後會講.
QQ: 我想問的問題.
Learn more: 其他optional的課程(很多是老師上過的其它課). 這門課學完後再回來看.
TODO: 未來花時間回來研究.

# 【機器學習2021】01 預測本頻道觀看人數 (上) - 機器學習基本概念簡介
https://youtu.be/Ye018rCVvOo?si=6usM-NGqmwlsbWKb

Def: **model**, **weight**, **bias**, **feature**, 
**Loss**: $\text { Loss: } \quad L=\frac{1}{N} \sum_n e_n$. Average of errors. **error**可以是mean absolute error (MAE), mean square error (MSE), Cross-entropy...
**label**(指的就是正確的數值。例句: 做機器學習都需要label.)
**Optimization**: $w^*, b^*=\arg \min _{w, b} L$. 這門課我們唯一會用到的方法就是Gradient Descent(GD).

**Gradient Descent**: 
$\left.\frac{\partial L}{\partial w}\right|_{w=w^0, b=b^0}$ => $w^1 \leftarrow w^0-\left.\eta \frac{\partial L}{\partial w}\right|_{w=w^0, b=b^0}$
$\left.\frac{\partial L}{\partial b}\right|_{w=w^0, b=b^0}$ => $b^1 \leftarrow b^0-\left.\eta \frac{\partial L}{\partial b}\right|_{w=w^0, b=b^0}$ , where $\eta$ : **learning rate**.$\eta$ : learning rate.
**hyperparameters**: 不會在Optimization(Training)更新值. 需要自己設定，不是讓機器自己去找出來的未知參數。像是這裡的$\eta$.
師: 常聽說Gradient Descent容易找到Local minima而不是Global minima. 這種Local minima的說法其實是假問題! Gradient Descent真正的痛點是什麼? 之後再教。
![[Pasted image 20251103183121.png]]
**Training**: 寫出含有未知數的函式(需要Domain knowledge) -> define loss function -> 解Optimization, 三個步驟合起來就是訓練。
![[Pasted image 20251103183217.png]]
**Linear model**: 本堂課一圖以蓋之: 太簡單，所有model bias問題大(不要跟上述的bias b混淆，是不同的東西)，下堂課會用ReLU, sigmoid來組成更有彈性的function.
# 【機器學習2021】02 預測本頻道觀看人數 (下) - 深度學習基本概念簡介
https://youtu.be/bHcJCp2Fyxs?si=uSrPpHlkR3J96ak3

Q: 老師在Slide的P23~26使用了HandWaving的方式: 組合一堆不同c, w, b參數(定義見下方)的HardSigmoid function -> 任意piecewise linear function ->  逼近piecewise continuous function. 證明了任何一維的連續函數都能用如下的HardSigmoid function來近似: $y=b+\sum_i c_i \operatorname{HardSigmoid}\left(\underline{b_i+w_i x_1}\right)$

**Sigmoid Function**: $\begin{aligned} y & =c \frac{1}{1+e^{-\left(b+w x_1\right)}}=c \operatorname{sigmoid}\left(b+w x_1\right)\end{aligned}$
**Hard Sigmoid Function**: 即Sigmoid Function取極限，即為兩個ReLU合成.
**Rectified Linear Unit (ReLU)**: (P47~48 )
![[Pasted image 20251103203345.png|650x500]]

New Model(更有彈性，應可逼近所有連續函數): 
(P30)Model從1維延伸到n維如下:
$y=b+w x_1$ -> $y=b+\sum_i c_i \operatorname{sigmoid}\left(b_i+w_i x_1\right)$
$y=b+\sum_j w_j x_j$ -> $y=b+\sum_i c_i \operatorname{sigmoid}\left(b_i+\sum_j w_{i j} x_j\right)$ , 注意: i: 1~M, j: 1~N, M, N不用是同一個整數。
PS: 但是直接延伸到n維沒給證明，試著自己證明n維的連續函數如何能被下面的函示疊加近似? Hint: 先想在2維，Locally任意的function是否可以被近似? 削蘋果法~
$y=b+\sum_i c_i \operatorname{HardSigmoid}\left(b_i+\sum_j w_{i j} x_j\right)$
下面會用$\theta$表示所有未知參數
![[Pasted image 20250915154720.png]]


![[Pasted image 20251103205231.png|700x400]]
現在gradient decent長這樣:  $\boldsymbol{\theta}^1 \leftarrow \boldsymbol{\theta}^0-\eta \boldsymbol{g}$, where $\boldsymbol{g}=\nabla L\left(\boldsymbol{\theta}^0\right) \quad$

Training with batch(P44 ):
**Batch**: 把dataset分成多個batch, 依序拿一個batch更新一次參數, 再拿下一個batch更新一次參數, ...
**Epoch**: Update所有Batch一次.
**Update**: 每個Batch看完更新一次參數叫做一次Update.
**Batch Size**(B): 也是一個HyperParameter.
例子說明: 10,000 examples (N = 10,000), Batch size is 10 (B = 10)
Q: How many update in 1 epoch? A: 1,000 updates

Sigmoid → ReLU: (因為Sigmoid是兩個ReLU疊加)
$y=b+\sum_i c_i$ sigmoid $\left(b_i+\sum_j w_{i j} x_j\right)$ -> $y=b+\sum_{2 i} c_i \max \left(0, b_i+\sum_j w_{i j} x_j\right)$
QQ: 這邊的式子似乎不嚴謹，因為合成Sigmoid的成對ReLU的參數應該不是獨立的，所以換成ReLU表示應該還是i組變數, 而不是2i組. (TODO: 有空時回來把式子寫對，找出成對的兩組ReLU的參數的關係式)
**Activation function**: sigmoid, HardSigmoid, ReLU. 使用者自由決定. 師: 用ReLU比Sigmoid好! 下周再講why. (To be taught)

![[Pasted image 20251103214544.png]]
n layers: 可以多做幾層, 定義如下圖: (層數又是另一個Hyper parameter.)
![[Pasted image 20250914195912.png]]
![[Pasted image 20251103215536.png]]
**Overfitting**: 如上例, 太多層數. 理論上層數加大可以趨近任何連續函數. 師: 要選三層還是四層好? 下周講 (To be taught)
Def: **Neuron**, **Neuron Network**, **hidden layer**, **Deep Learning**: Deep = Many hidden layers.
![[Pasted image 20251103214744.png]]
P57 Why we want “Deep” network, not “Fat” network? 為什麼我們不選擇把network變胖而是變深呢? 未來課程回答. (To be taught)

Learn more:
Basic Introduction: 舊版的Deep Learning介紹
https://www.youtube.com/watch?v=Dr-WRlEFefw
Backpropagation: Computing gradients in an efficient way
https://www.youtube.com/watch?v=ibJpTrp5mcE

QQ: 每一層都是把該層的a拿去跟y算Loss(a, y)來逐層的決定(b, w)的最佳化值嗎? 還是每一次updater就同時更新了所有層的(b, w)?
# 【機器學習2021】03 # 機器學習任務攻略
https://youtu.be/WeHM2xpYQpw?si=GdNzJ2wa5rqBb355

本課程: 把前期作業做好的一些基本招式.
![[Pasted image 20251103220632.png]]

![[Pasted image 20251103220650.png]]

先檢查Training data loss => 看model有沒有學起來
Training Loss太高 => 辨別問題癥結:
Model Bias: Model不夠複雜 => 增加Model彈性。
Optimization: Model已經足夠複雜，是找最佳解的時候出問題，例如可能是Gradient Descent這個方法卡在Local Minimum的問題。(To be taught): 如何解決Optimization的問題.

彈性大=>卻沒有變好. 檢查是Overfitting還是Optimization的問題! (如下圖展示的例子)
![[Pasted image 20251103221134.png]]
Tips: 任何問題都先從LR或是SVM開始(比較沒有Optimization問題)，再換成複雜的model，跟前面的初階通用Model比較，結果如果比較差，就可以推測複雜的model可能是有最佳化問題。
Overfitting: Training loss小, testing loss大. （train loss ↓，但 dev loss ↑）
Optimization的問題: Training loss沒有隨著model變複雜而跟著變小. 可能的解法:
1. More training data
2. Data augmentation (you can do that in HWs): 增加(人工產生的)資料。需要有Domain Knowledge當guide，來產生合理的fake資料。
3. 給model更多限制: 運用Domain knowledge
4. constrained model: (根據Domain Knowledge)限制你的model自由度:  •特定形式的function •Less parameters, sharing parameters(後面CNN會教到) • Less features • Early stopping(助教有寫在作業裡) • Regularization(自己survey) •Dropout(之後會提到)
來克服 (but cannot do it in HWs).

Bias-Complexity Trade-off: 如何選到最好的中庸model? => Cross Validation:
![[Pasted image 20251103223938.png|400]]
![[Pasted image 20251103223133.png]]
**public testing set vs private testing set**: 真實世界很多案例表現很好其實都是在public testing set上做的. 但你用太暴力的使用public testing set來挑選你的model往往會得到在private testing set上比較差的結果.
師: 其實最好的做法就是挑validation loss最小的model就好, 越少看public testing loss 越好. (但從我前後聽起來，可能"稍微"的使用public testing set來挑選model會更好，只是老師不希望大家往那個方向去努力.)

**Validation**:  **N-fold Cross Validation**: 減少Validation set分的不好的可能.
**Mismatch**(HW11): Training data 跟 Testing date的分佈不一樣，此時跟overfitting不同，增加訓練資料沒有幫助。 師: 我如何知道現在是不是Mismatch呢? 需要看你對資料本身的理解.
# 【機器學習2021】04~05 # # 類神經網路訓練不起來怎麼辦： 局部最小值 (local minima) 與鞍點 (saddle point) / 批次 (batch) 與動量 (momentum)
https://youtu.be/QW6uINn7uGk?si=DLGu8ZkFLmWX2N_6
https://youtu.be/zzbr1h9sF54?si=BrYqoKnnigO_mrxB
Critical point: Gradient = 0 => Saddle point or local minimum maximum.
檢查Hessian的eigen value來判斷是何種。
算出Hessian => 沿著負Eigenvalue的Eigenvector => Loss變小。但實務上Hessian因為計算量大，通常沒在用。
研究上發現因為Model的參數/維度很大，通常都是卡在SaddlePoint而不是LocalMinimum。 

Smaller batch size and momentum help escape critical points:
Batch:
small Batch => Optimization(training data)，Generalization(test data)比較好!
 => (Training) Time for one epoch 比較快!
課後閱讀: slide上cite一些研究，運用一些技巧來使用large Batch並且避免large Batch的劣勢。

Momentum:
## (Vanilla) Gradient Descent:
![[Pasted image 20250913162615.png]]
Starting at $\boldsymbol{\theta}^{\mathbf{0}}$
Compute gradient $\boldsymbol{g}^{\mathbf{0}}$
Move to $\boldsymbol{\theta}^{\mathbf{1}}=\boldsymbol{\theta}^{\mathbf{0}}-\eta \boldsymbol{g}^{\mathbf{0}}$
Compute gradient $\boldsymbol{g}^{\mathbf{1}}$
Move to $\boldsymbol{\theta}^{\mathbf{2}}=\boldsymbol{\theta}^{\mathbf{1}}-\eta \boldsymbol{g}^{\mathbf{1}}$

## Gradient Descent + Momentum:
![[Pasted image 20250913162740.png]]
Starting at $\boldsymbol{\theta}^{\mathbf{0}}$
Movement $\boldsymbol{m}^{\mathbf{0}}=\mathbf{0}$
Compute gradient $\boldsymbol{g}^{\mathbf{0}}$
Movement $\boldsymbol{m}^{\mathbf{1}}=\lambda \boldsymbol{m}^{\mathbf{0}}-\eta \boldsymbol{g}^{\mathbf{0}}$
Move to $\boldsymbol{\theta}^{\mathbf{1}}=\boldsymbol{\theta}^{\mathbf{0}}+\boldsymbol{m}^{\mathbf{1}}$
Compute gradient $\boldsymbol{g}^{\mathbf{1}}$
Movement $\boldsymbol{m}^{\mathbf{2}}=\lambda \boldsymbol{m}^{\mathbf{1}}-\eta \boldsymbol{g}^{\mathbf{1}}$
Move to $\boldsymbol{\theta}^{\mathbf{2}}=\boldsymbol{\theta}^{\mathbf{1}}+\boldsymbol{m}^{\mathbf{2}}$
Movement not just based on gradient, but previous movement.

# 【機器學習2021】06~ # # 類神經網路訓練不起來怎麼辦：自動調整學習速率 (Learning Rate)

https://youtu.be/HYUXEeh3kwY?si=STswcAHcyFFZIRhL


Gradient Descent往往很難走到norm of Gradient很接近0的位置(critical point)。
GD -> RMSProp -> Adam = RMSProp + Momentum -> + Learning Rate Scheduling
Learning Rate Scheduling: $\boldsymbol{\theta}_i^{\boldsymbol{t}+\boldsymbol{1}} \leftarrow \boldsymbol{\theta}_i^{\boldsymbol{t}}-\frac{\boldsymbol{\eta^t}}{\sigma_i^t} \boldsymbol{g}_i^{\boldsymbol{t}}$
A. Learning Rate Decay: 最後已經接近終點時可以走慢點，避免最後震盪。
![[Pasted image 20250913221016.png]]
B. Warm Up: 有效的原因待研究。可能的解釋: 一開始用來計算$\sigma$的點不夠多，統計誤差大。等經過的點多了，計算出來的$\sigma$統計上更可靠時再走快一點。
![[Pasted image 20250913221025.png]]
課後閱讀: 更多關於的Optimization東西
[TA 補充課] Optimization for Deep Learning
https://www.youtube.com/watch?v=4pUmZ8hXlHM
https://www.youtube.com/watch?v=e03YKGHXnL8
## My Reflections
**GD**:  $\theta_{t+1}=\theta_t-\eta \nabla_\theta J(\theta)=\theta_t-\eta g_t$
如果梯度大 → 走得快；梯度小 → 走得慢。缺點：梯度小時會走得慢，無法收斂到Local minimum(不確定?)
![[Pasted image 20250913214201.png]]
**NGD**(Normalized Gradient Descent): $\theta_{t+1}=\theta_t-\eta \frac{g_t}{\left\|g_t\right\|}$
不管梯度多大，永遠走「固定長度」的一步。。優點：避免梯度爆炸。缺點：忽略梯度大小，收斂慢，可能會在谷底兩側震盪(?)
**RMSProp**: $\boldsymbol{\theta}_i^{\boldsymbol{t}+\boldsymbol{1}} \leftarrow \boldsymbol{\theta}_i^{\boldsymbol{t}}-\frac{\eta}{\sigma_i^t} \boldsymbol{g}_i^{\boldsymbol{t}} \quad \sigma_i^t=\sqrt{\alpha\left(\sigma_i^{t-1}\right)^2+(1-\alpha)\left(\boldsymbol{g}_i^{\boldsymbol{t}}\right)^2}$

QQ: 無法理解這樣為什麼比較好。為什麼不是用更直觀的全域修正的方式如下:
$\theta_i^{t+1} \leftarrow \theta_i^t-\frac{\eta}{\sigma^t}, \sigma^t=\sqrt{\alpha\left\|\sigma^{t-1}\right\|^2+(1-\sigma)\left\|g^t\right\|^2}$
簡言之，全域修正($\left\|\boldsymbol{g}^{t-1}\right\|^2$) vs. per-parameter($(g_i^{\boldsymbol{t}})^2$)) 修正: 讓個別的parameter自己去調整為何比較好?

Momentum: $v_t=\beta v_{t-1}+(1-\beta) g_t, \quad \theta_{t+1}=\theta_t-\eta v_t$
這個版本比老師教的多了一個$\beta$自由度，覺得應該是比較好的版本。

# 【機器學習2021】07 # 類神經網路訓練不起來怎麼辦 (四)：損失函數 (Loss) 也可能有影響
https://youtu.be/O2VkP8dJ5FE?si=3p9YEgszC-8Pf1U8

建議閱讀過去更完整版的Classification教學:
https://youtu.be/fZAZUYEeIMg?si=rzaC3rghEbkU201N
https://youtu.be/hSXFuypLukA?si=62MqJx02mXiy-CkV

**x**經NNs(Neural Network)得到**y**(實數)，再追加經過一層softmax得到y'(機率向量)
之後這裡定義Loss as Cross-entropy: $e=-\sum_i \widehat{\boldsymbol{y}}_i \ln \boldsymbol{y}_i^{\prime}$
會比MSE(Mean Square Error)更好。時間有限，課程中只給了Handwaving的解釋。
# 【機器學習2021】08 類神經網路訓練不起來怎麼辦 (五)： 批次標準化 (Batch Normalization) 簡介
https://youtu.be/BABPWOkSbLE?si=Nv7ldK5z5lrZZf5I

Batch normalization: (每個batch進行計算時)對每一層的input xi跟Activation function前(或後)的參數各做一次standard normalization. 若Activation function是sigmoid, 建議要在Activation function前的參數做Normalization比較好.
Inference: 就是testing.
Batch normalization – Testing: testing data要一筆一筆即時算, 沒有batch, 無法計算(σ, µ). Computing the moving average of 𝝁 and 𝝈 of the batches during training 用來給testing data使用.
延伸閱讀: Batch normalization如何用在CNN上? 參考Original paper: https://arxiv.org/abs/1502.03167

QQ: 聽無，這段的(γ, β)是啥? 怎麼決定? 為什麼不是用原本的(σ, µ) (layer by layer 的)回推回第一層?
https://youtu.be/BABPWOkSbLE?list=PLJV_el3uVTsMhtt7_Y6sgTHGHp1Vb2P2J&t=1083
$$
\begin{aligned}
& \tilde{z}^i=\frac{z^i-\mu}{\sigma} \\
& \hat{z}^i=\gamma \odot \tilde{z}^i+\beta
\end{aligned}
$$

![[Pasted image 20250915224154.png]]
To learn more: (about Renormalization)
• Batch Renormalization • https://arxiv.org/abs/1702.03275 
• Layer Normalization • https://arxiv.org/abs/1607.06450 
• Instance Normalization • https://arxiv.org/abs/1607.08022 
• Group Normalization • https://arxiv.org/abs/1803.08494
• Weight Normalization • https://arxiv.org/abs/1602.07868 
• Spectrum Normalization • https://arxiv.org/abs/1705.10941

# 【機器學習2021】09 卷積神經網路 (Convolutional Neural Networks, CNN)
https://youtu.be/OP5HcXJg2Aw?si=FuiWNu2ARWH7oCDT

CNN: 特化來影像處理的.
本課程用兩個觀點去理解CNN
Neuron Version Story: Each neuron only considers a receptive field. The neurons with different receptive fields share the parameters.
Filter Version Story: There are a set of filters detecting small patterns. Each filter convolves over the input image.
PS: Neuron Version Story中一組sharing parameter就是Filter Version Story中的一個filter!

Fully Connected Network: 每個Neuron都跟input的每一個dimension有一個weight(連接)
Observation: A neuron does not have to see the whole image (to identify a pattern).
Receptive field: 每個neuron只管一個有限的範圍(Receptive field). 不同neuron的Receptive field可以重複，可以全同. 每個neuron的Receptive field通常是連接的範圍, 如果符合你的需求也可以用不連接的範圍.
Typical Setting: 管色彩的3-channels總是全部包含. 自由度: kernel size(長寬), stride(平移量), padding(邊緣補值: 給處理到最邊緣的Receptive field使用). parameter sharing:  不同的Receptive field共享全同參數, 概念上代表在不同區域偵測相同的pattern, Two neurons with the same receptive field would not share parameters.

Fully Connected Layer -> 簡化自由度: Receptive Field + Parameter Sharing = Convolutional Layer.
CNN(Convolutional Layer Network) 

Pooling: 刪除偶數行的column跟奇數行的row, 用來把圖縮小(成1/4), 減少計算量, 但是會影響performance. Max/min/mean Pooling. 近年來如果計算能力夠, 也可以不用. optional.

Alpha Go: 使用CNN, 但沒有使用Pooling! 因為Pooling不適合圍棋.

 CNN is not invariant to scaling and rotation (we need data augmentation ☺)! CNN的架構, 不支援放大縮小旋轉(但平移可).  Why? 有空多想想.

 To learn more: 
Spatial Transformer Layer:  invariant to scaling and rotation.
https://www.youtube.com/watch?v=SoCywZ1hZak


# 【機器學習2021】10 # 自注意力機制 (Self-attention) (上)
https://youtu.be/hYdO9CscNes?si=W0UyrZPFuC5nw3NH

One-hot Encoding
![[Pasted image 20250914223213.png]]

Word Embedding
![[Pasted image 20250914223230.png]]
 To learn more: **Word Embedding** https://youtu.be/X7PH3NuYW0Q


• Each vector has a label. (focus of this lecture)
• The whole sequence has a label. (HW4)
• Model decides the number of labels itself. (HW5)


法1: 最陽春, 無法解決 "I saw a saw" 的label詞性問題.
![[Pasted image 20250914225615.png]]

法2: window. 你不容易知道window大小足夠大是說多大. 如果window設為整個sequence計算量太大, 且容易overfitting.
![[Pasted image 20250914225842.png]]

法3: Self-attention. 先從整個sequence of vector, 算出一樣大小的sequence of vector. 這樣可以看過整個sequence資訊.
![[Pasted image 20250914230112.png]]
計算細節:
老師說也不一定要用Dot-product, 可以用其他選擇. 課堂上舉例了另一種Additve選擇. (不過我覺得要比較兩個向量的相似程度, 還是用Dot比較直覺.)
![[Pasted image 20250914230624.png]]
![[Pasted image 20250914230610.png]]
![[Pasted image 20250914230823.png]]

師講解: 因為$\alpha$是機率向量, 最後b的值會趨近機率dominant對應的那個$v_i$分量.
QQ: 最後從v算b的意義是什麼? 到底是在幹嘛?
# 【機器學習2021】11 # 自注意力機制 (Self-attention) (下)
https://youtu.be/gmsMY5kc-zw?si=NhomTXgXqPXk6kXe

Multi-head Self-attention: 為了算$q^{q,1}$, $q^{q,2}$, 除了原本的$W^q$還多了$W^{q,1}$, $W^{q,2}$(舉2head為例)
![[Pasted image 20250915011136.png]]


QQ: 為什麼不乾脆改成下面這種架構, 這樣算$q^{q,1}$, $q^{q,2}$, 就只需要$W^{q,1}$, $W^{q,2}$. 這樣不是比較簡潔, 而且model跟上面的架構是等價的不是嗎?
![[Pasted image 20250915011644.png]]


Positional Encoding: 為了增加位置資訊, 多加了一個hand-crafted positional vector $e^i$
![[Pasted image 20250915012151.png]]


Q: 如果是我的話, 會直觀的改在dot product, 把$q^i k^{i'}$魔改加入depend on 兩點距離的decay multiplier, 像是 $q^i k^{i'} e^{-|i-i'|}$. (直覺: 距離越遠關係越小.) 但這個方法只能引進相對距離的資訊, 不像Positional Encoding可以引進絕對距離的資訊.
TODO: 未來可以嘗試實踐我的上述做法, 看在某些問題上會不會變得比較好. 也可以把這個作法用在下面的"把Self-Attention用在影像處理"的問題試驗看看效果. (此時, decay depend on 兩個pixel在圖上的二維距離). PS: 可能跟第13堂課提到的Guided Attention有關.

Truncated Self-attention: 處理像是語音這種vector數量很大的data, 考慮全部計算量會太大, 改成人為的切段只考慮局部.

Self-Attention GAN(https://arxiv.org/abs/1805.08318): 把Self-Attention用在影像處理, 每一個pixel當作一個長度3(RGB 那個color channel)的vector.

Self-Attention: 每一個pixel考慮跟其他所有pixel之間的相關資訊. 容易overfit, 但data量大時可以做得比CNN好.
CNN: 只考慮同一個receptive field裡的pixel的資訊. Good for less data.
=> CNN 是簡化版的 self-attention.
事實上, 可以證明CNN是Self-Attention的特例.
On the Relationship between Self-Attention and Convolutional Layers https://arxiv.org/abs/1911.03584

(hw4)  要用CNN還是Self-Attention呢? 老師提示要用conformer(Q: 這啥?), 既有用到CNN也有用到Self-Attention.


Self-attention v.s. RNN: 
RNN: 因為近來逐漸被Self-attention取代, 這門課就只在這裡簡短提一下跟Self-attention做比較, 未來不會再提.
缺點: 中間那個架構不能平行處理. 單向的RNN每個節點只能考慮之前節點的資訊, 不像Self-attention可以考慮整個sequence的資訊, 雖然雙向版本的RNN可以同時考慮前後, 但是舉極端例子, 最左邊節點的資訊要從最左的memory開始, 一層層的往右傳遞並且不失去資訊, 才能讓最右邊的端點獲得資訊. (Reflections: 這跟我上面的TODO裡想像的距離decay也有有雷同之處?)

![[Pasted image 20250915151822.png]]

To learn more: Self-attention 加上一些什麼就會變成 RNN. https://arxiv.org/abs/2006.16236

To learn more: RNN過去的上課教學. (因為老師已經講Self-attention Win! 所以建議暫時可以先不看)
https://www.youtube.com/watch?v=xCGidAeyS4M
https://www.youtube.com/watch?v=Jjy6ER0bHv8

Self-attention for Graph: 把CNN用在graph上面是某一種變形的GNN
Attention Matrix的資訊不需要用learn的得到, 可以直接採用edge提供的資訊, 每個節點只考慮有連接的點的相關資訊即可. (Reflection: 是不是可以依照連接深度去分層考慮, 讓連接距離不同的節點重要性可以不一樣? Ex: 考慮下圖點1, 有一個"1-層Attention Matrix": 考慮點5,6,8, 有另一個"2-層Attention Matrix": 多考慮點3,4,7...")
![[Pasted image 20250915153030.png]]

To learn more: GNN
https://www.youtube.com/watch?v=eybCCtNKwzA
https://www.youtube.com/watch?v=M9ht8vsVEw8

廣義的Transformer就是Self-attention: 很多人在講Transformer時就是在說Self-attention.
Self-attention有很多變形都會被命名成XX-former.
To learn more: (短期不用看, 有興趣再說.) Self-attention如何做得更快更好仍然尚待研究. Efficient Transformers: A Survey https://arxiv.org/abs/2009.06732

# 【機器學習2021】12 Transformer (上)
https://youtu.be/n9TlOhRjYoc?si=6Zt1DVhPFCv9xw7F

input Sequence時, 三種不同output的Case:
1. (hw2)output 一樣大的Sequence
2. (hw4)一個東西
3. Seq2Seq: 長度讓模型自己決定的sequence: 語音辨識, 文字/語音翻譯, 聊天機器人. (To learn more: 課程裡有提供一些台語語音處理的相關source.)

To learn more: 什麼問題適合選用哪一種客製化的models, 參考這門課. (雖然有可能超出這門課的範圍, 以後可以回來看, 看能不能知道例如量化可以用什麼模式比較合適.)
Deep Learning for Human Language Processing 2020 Spring
Source webpage: https://speech.ee.ntu.edu.tw/~hylee/dlhlp/2020-spring.html

一些應用:
Seq2seq for Multi-label Classification: https://arxiv.org/abs/1909.03434, https://arxiv.org/abs/1707.05495
Seq2seq for Object Detection: https://arxiv.org/abs/2005.12872

Transformer架構: Encoder -> Decoder

Encoder架構: you can use RNN or CNN. Positional Encoding, Multi-Head Self-Attention, Add & Norm: Residual(output+input來作為output.) + Layer norm, Feed Forward: FC network.

To learn more: 為什麼用layer Normalization比Batch Normalization好? 參考這篇文章
PowerNorm: Rethinking Batch Normalization in Transformers https://arxiv.org/abs/2003.07845
# 【機器學習2021】13 Transformer (下)
https://youtu.be/N6aRv06iv2g?si=zOT3IR1uUYL8Az3B

Encoder: 有兩種Autoregressive(AT)跟Non-Autoregressive(Non-AT)，這堂課主要講AT
Error propagation: 一步錯，步步錯
Masked Self-attention: 每個點計算時只考慮之前的點。因為Decoder 的 input是一個接一個，後面的input在一開始還沒產生。

NAT: (1) 另外用一個classifier (2) 固定長度 => 來決定輸出的sequence長度。
AT VS NAT: NAT 可以平行化。NAT的performance比AT差(Why? Multi-modality)。 Learn more: NAT https://youtu.be/jvyKmU4OM3c?si=0HKzKyff2457L0-0

Cross Attention: 原始paper裡，Decoder吃Encoder的最後一層輸出。但不一定總是要這樣，講義有列出其他研究的不同方案。
![[Pasted image 20251026013626.png]]

Training: minimize cross entropy between Ground truth (標準答案，is one-hat vector) and output distribution (師: 很像是分類問題!).1
Teacher Forcing: using the ground truth as input. PS: 但是在訓練完成之後要推論時，這裡的input沒有標準答案可以代入，所以會有**Mismatch**的問題(用後面提到的Scheduled Sampling處理)。
![[Pasted image 20251026014233.png]]
Tips: 
1. Copy Mechanism: output不用全部自己產生，而是直接從input copy，像是summary. Learn more: https://youtu.be/VdOyqNQ9aww?si=RVXIx8k1_8iOXip7

2. Guided Attention: Monotonic Attention，Location-aware attention 。(TODO) Learn more! 我覺得這個可能是我會用到的實用技巧! 近未來可以深入研究。

3. Beam Search: Greddy Decoding最快速簡單但不是最佳，但完整解又太複雜。利用某種approximated solution -> Beam Search(有時候有效(有精確答案的Case，像是語音辨識。)，有時候無效(需要發揮創造力的Case，需要加入隨機性。)。). Learn more: 老師沒講，有興趣自己學。
4. Sampling: TTS: 在測試的時候加入noise!
5. Optimizing Evaluation Metrics: 訓練的時候我們是minimize cross-entropy，但validataion的評估是用maxize BLEU score。但因為BLEU score不可微分，沒辦法拿來做optimize，因為GD不能計算。Tips: 遇到Optimize無法解決的問題，用RL硬Train一發就對了! (這是個比較難的作法，沒有推薦在作業裡使用。)
6. Scheduled Sampling: 改善Mismatch問題，在訓練的時候加入一些錯誤的input，因為在測試時，Decoder本來就有可能產生錯誤的output餵給input。
# 【機器學習2021】14 生成式對抗網路 (Generative Adversarial Network, GAN) (一) – 基本概念介紹
https://youtu.be/4OWp0wDu6Xw?si=XPaM2GvuqcOpruPL

Generator: 輸出一個分布。input 加入 distribution參數，則輸入會變成(一個比較複雜的)distribution。
需要的場合: 需要"創造力"的任務。同樣的輸入，可以有多種可能的輸出，而且每種輸出都是對的。
Discriminator: input圖片，output scalar，scalar越大表示越正確(可以當作regression或是分類問題。)。可以用CNN，transformer等方法實現。

Algorithm
Step 1: Fix generator G, and update discriminator D
Step 2: Fix discriminator D, and update generator G
Learn more: https://zhuanlan.zhihu.com/p/24767059
![[Pasted image 20251026170958.png]]


# 【機器學習2021】15 生成式對抗網路 (Generative Adversarial Network, GAN) (二) – 理論介紹與WGAN
https://youtu.be/jNY1WBb8l4U?si=YeDHQLfIb0-dBnjU
目標: 
$$
G^*=\arg \min _G \operatorname{Div}\left(P_G, P_{\text {data }}\right)
$$
Divegence:  P_G, P_data的"差異"。一堆積分無法算=>可以先各自隨機 sampling出data，再用這些data來算近似值，如以下說明:

Train: By Objective Function
$$
\text { Training: } D^*=\arg \max _D V(D, G)
$$
PS: 
當我們要maxmize一個東西，我們叫他Objective Function
當我們要minimize一個東西，我們叫他Loss Function

Objective Function: (不一定要但)一開始設計成這樣是為了等價分類問題，maximize這個Objective Function等於minimize cross-entrophy(QQ: 我好像看不出來，怎麼證明?)
$$
V(G, D)=E_{y \sim P_{\text {data }}}[\log D(y)]+E_{y \sim P_G}[\log (1-D(y))]
$$
用這個Objective Function train完後，$$\max _D V(D, G)$$這個值就當作Divegence! 這個值不會完全等價但會跟JSDivegence有關。Learn more(前述的證明): https://arxiv.org/abs/1406.2661

$$
G^*=\arg \min _G \max _D V(G, D)
$$

Why 我們要用一個跟JSDivegence相關的Objective Function，而不是直接用JSDivegence或其他種Divegence來定義Objective Function?
A: Actually, you can use the divergence you like。以下paper有告訴你可以使用其他各種Divegence，但結果也沒有因此變好。Learn more: https://arxiv.org/abs/1606.00709

GAN is difficult to train!! GAN是以不好train聞名的!
Tips for GAN:

What is the problem of JS divergence? A: In most cases, 𝑃𝐺 and 𝑃𝑑𝑎𝑡𝑎 are not overlapped. (老師有解釋兩個原因，主要是因為構成"2D人物圖形"的集合佔所有可能的像素集合太小。)=> JS divergence is always log2 if two distributions do not overlap.  Intuition: If two distributions do not overlap, binary classifier achieves 100% accuracy. The accuracy (or loss) means nothing during GAN training.
![[Pasted image 20251026235627.png]]
WGAN: 用Wasserstein distance來取代JSDivegence 的 GAN.
Wasserstein distance: W(P, Q)=從P move to Q 需要的距離。moving plans有很多，我們要取最小距離的moving plans. (老師表示他這邊沒有給證明: 但下面的這個值解出來就是Wasserstein distance.)
$$
\max _{D \in 1-\text { Lipschitz }}\left\{E_{y \sim P_{\text {data }}}[D(y)]-E_{y \sim P_G}[D(y)]\right\}
$$
1−𝐿𝑖𝑝𝑠𝑐ℎ𝑖𝑡z: 某種function要足夠平滑的要求，不然會產生極端不連續的function來讓上面的optimize總是無限大。

WGAN怎麼算? 老師講了三種方法Weight, Gradient Penalty, Spectral Normalization.
但老師說Spectral Normalization才是真的1−𝐿𝑖𝑝𝑠𝑐ℎ𝑖𝑡z的限制，目前看起來效果最好 → Keep gradient norm smaller than 1 everywhere.
TODO: 未來可能會有需要使用Spectral Normalization.  Learn more: https://arxiv.org/abs/1802.05957
QA時間:
Q1: 在Update/Train 這輪的Discriminator時，能不能用過去Update的資料? A: 理論上沒有用，實務上可以用，因為過去的資料拿來用資料量會比較大，有可能會更robust. 另外，其實在Train Discriminator時，實務上沒有人會真的跑到optimize收斂，都是Discriminator跑個幾次，就換去算Generator也跑個幾次，效率考量。

# 【機器學習2021】16 生成式對抗網路 (Generative Adversarial Network, GAN) (三) – 生成器效能評估與條件式生成

GAN很難Train: Generator跟Discriminator必須都能一直進步，如果哪一個沒辦法Train得更好，另一個也就會停下來(之後可能必須嘗試調hyperparameter)。
output token 是取 max，Decoder一點的微小變化沒辦法改變輸出! 不能用Gradient decent! 一個可能是用RL來取代optimization, 但是RL is difficult to train，加上原本GAN is difficult to train => 很長一段時間沒有人能Train好Generator for 產生文字.
Q: Why CNN用Max Pooling可以用Gradient decent? 師: 留給大家自己深思一下.

ScarchGAN: It can train from scratch with enough hyperparameter-tuning and tips,

Learn more: TODO: (近未來應該會用到) GAN要學得更完整，看老師以前的lecture: Generative Adversarial Network (GAN): https://youtu.be/0CKeqXl5IY0?si=VLOhtqzTKF2r3iGQ

Learn more:
More Generative Models:
Variational Autoencoder (VAE): https://youtu.be/8zomhgKrsmQ?si=regD7gvLU2oEoSzl
FLOW-based Model: https://youtu.be/uXY18nzdSsM?si=dmpJ4ZRbDyVLKrt3
用Supervise learning的方法能不是對抗的方法去做: 
Generative Latent Optimization (GLO), https://arxiv.org/abs/1707.05776 
Gradient Origin Networks, https://arxiv.org/abs/2007.02798
![[Pasted image 20251027014641.png]]
How to evaluate the quality of the generated images automatically? A1: 一張 image -> Image Classifier -> distribution. => Concentrated distribution means higher visual quality(代表這張圖被歸類在人臉class). 但還是有下述問題，即便到現在都還沒有很好的解決方案。
Mode Collapse:
![[Pasted image 20251027015307.png]]
Mode Dropping:
![[Pasted image 20251027015326.png]]

A2: 換成1000張 image -> Image Classifier -> distribution 的平均. => 看 distribution 是不是平均的分佈在不同的Class裡 => large diversity.
Inception Score (IS): Good quality, large diversity → Large IS
但這個方法不是用這裡的2D人臉問題，因為輸出都是同一個class. 所以我們的作業用是用 Fréchet Inception Distance (FID) 如下: (取softmax之前的向量，而不是之後的值是因為softmax之後決定的最後的類別幾乎都是一樣，但是softmax決定類別之前的向量，是會不一樣的。用那個向量的分布來跟real images的分佈來做比較。)
師: 作業評估採用(1)FID跟(2)辨識出來的人臉數目的混合評分。單用FID效果會不好，因為分布不是Guassian。
https://youtu.be/MP0BnVH2yOo?list=PLJV_el3uVTsMhtt7_Y6sgTHGHp1Vb2P2J&t=2168
事實上評估GAN的好壞非常困難，例如模式可以只是複製輸入圖片或是其鏡像來生成，則FID會極低，但它無法產生新圖片，事實上不是好的模型。
![[Pasted image 20251027020333.png]]
My remarks: FID用在比較兩個Guassian distribution時，跟Wasserstein distance是相同的。要記得老師上課提到的，用來非Guassian分佈可能會不理想。

**Conditional Generation:** 舉text to image為例。要有paired data。下面的Cycle GAN，可以使用unpaired data。
![[Pasted image 20251101171033.png]]

![[Pasted image 20251101171113.png]]
Discriminator也要輸入x(text), 如果只有y(生成圖片)則無法判斷圖片是否match輸入。
Train要使用標註data如下: 不能只有(正確圖，1)，(錯誤圖，0)，訓練的效果會不好，還要加上(正確圖，0)，結果才會好。
![[Pasted image 20251101173311.png]]


![[Pasted image 20251101173226.png]]
(1) supervised learning: https://youtu.be/4OWp0wDu6Xw?list=PLJV_el3uVTsMhtt7_Y6sgTHGHp1Vb2P2J&t=354
參考老師講的小精靈例子，會輸入"可能的平均"，變得模糊。不適合有"創造力"的問題。
(2) GAN: 圖看似比較好，但是有時會創造出奇怪的東西，像是屋頂多了不知道是不是煙囪的東西。
(3) GAN+supervised learning: 同時追求兩個目標(怎麼做? 老師沒講。TODO: 近未來自己學，可能很有用。)。(1) Discriminator騙過(保持創造力)的同時，又(2)(supervised learning)盡可能的產生跟標準答案越像越好的圖 => 結果最好。

其他的Conditional GAN應用: Sound-to-image，Talking Head Generation。

# 【機器學習2021】17 生成式對抗網路 (Generative Adversarial Network, GAN) # (四) – Cycle GAN
https://youtu.be/wulqhgnDr7E?si=Sh5rCccX3db4rOR-

Unpaired data 未標註資料: (Hw3, Hw5或多或少都還是需要一點成對的資料。)

一種應用: 影像風格轉換

**Cycle GAN:**
原理: 兩層G，G1: x-> y，G2: y-> x。兩步驟訓練: D: 判斷G1的輸出是否在y domain，G2: 輸出要跟G1的圖片越像越好(用兩張圖片的距離還評估，很簡單。)
![[Pasted image 20251101175941.png]]

Q: 這樣的訓練條件是否不夠充分，似乎無法保證風格轉換真的有學到足夠正確的特徵? A: 理論上確實是，但目前沒有很好的解法。理論上像是有可能會學到G1: 把眼鏡換成痣，G2: 痣又換回眼鏡。但實務上訓練的效果通常就不錯，往往不會發生問題，因為模型很偷懶，會想要盡量跟原圖越像越好，不喜歡太複雜的轉換。甚至只用原本的G1訓練不用Cycle GAN 也能做得不錯!
GAN的訓練是雙向的: QQ: (老師沒講細節) 雙向的訓練都要同時進行才行? 或是兩個方向擇一來做就可以?
![[Pasted image 20251101181838.png]]

Text Style Transfer 文字風格轉換: Discriminator不適合用text sequence(參考第16堂課開頭)，要改成用RL硬做。



# 【機器學習2021】18-19 自督導式學習 (Self-supervised Learning) (一) – 芝麻街與進擊的巨人 (二) – BERT簡介
https://youtu.be/e422eloJ0W4?si=KiOdv1I7XU3bXm4T
https://youtu.be/gh0hewYkjgo?si=c3c8GjOz22Y6yEGE

Self-supervised Learning: 一種Unsupervised Learning
 Supervised Learning for paired data vs Unsupervised Learning for unpaired data
 一開始有兩種用途:
 1. Masking Input Prediction填字問題: Mask的方式也是Train時隨機決定。![[Pasted image 20251101184423.png]]
 2. Next Sentence Prediction (不太有用): 用來判斷一個句子是否市接在另一個句子之後? Yes/No.
 3. Downstream Tasks: 用在其他真正有用的任務。
 
Downstream Tasks ![[Pasted image 20251101185315.png]]

 pre-train: 產生BERT的過程。
 fine-tune: 微調BERT，讓其可以用來執行其他任務。
 Hw7 可以使用pre-train的model
 General Language Understanding Evaluation (GLUE): 用來測試Self-supervised的能力的一種方法。9種面向的任務。
 Case1: 
 這算是Supervised還是Unsupervised? 這算是Semi-Supervised。
 Semi-Supervised: Train使用大量無標註data (pre-train) +少量標註date(Train 後面的 linear 轉換那層的參數。)
 ![[Pasted image 20251101190326.png]]
Case2: sequence to (the same size) sequence: 句子詞性分析。
Case3: two sequences (前提，猜想)-> a class (中性/正確/矛盾)。TODO: 可以用在討論串分析正面/負面，贊同/反對。近未來有可能我會用到。
Case4: hw7
Q: document的輸入大小有沒有限制? A: 理論上沒有，但實務上有。BERT是一個Transformer encoder。...
# 【機器學習2021】20

# 【機器學習2021】18

# 【機器學習2021】18

# 【機器學習2021】18